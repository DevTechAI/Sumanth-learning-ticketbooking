spring.application.name=basic

# Example SMTP settings. Prefer environment variables for real credentials.
# For Gmail, SMTP_PASSWORD must be an app password, not your normal account password.
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=venkatamvs90@gmail.com
spring.mail.password=ujdi ronb ehtv anil
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.mail.from=venkatamvs90@gmail.com

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
